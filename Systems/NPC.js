import Matter from "matter-js";

const NPC = (entities, { touches, time, dispatch, events, screen }) => {
  let engine = entities.physics.engine;

  let zombie = entities.Zombie;

  return entities;
};

export default NPC;
